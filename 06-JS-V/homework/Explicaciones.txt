Prototype: En prototype básicamente esta la base de los objetos que creamos o los que ya vienen predefinidos por javascript, en el encontraremos funciones o valores a los que podremos acceder a traves de un punto, por ejemplo si intentaramos ejecutar una funcion en un objeto "nombreObjeto.nombreFuncion()" lo primero que pasaria seria que se buscaria la propiedad "nombreFuncion" dentro del objeto, y si no se encontrara se buscaria posteriormente en el prototype del objeto y se ejecutaria, y si no se encontrara ahi y el prototype de nuestro objeto perteneciera a otro prototype tambien se buscaria ahi, en ultima estacia si no se encontrara esa propiedad en ningun lado nos tiraria un error.

Constructors (de Clases): Una clase basicamente es una plantilla desde la que podremos crear objetos que compartan los mismos atributos y funciones, se declaran de forma muy parecida a la funciones, y dentro de la funcion se comporta como si crearas un objeto solo que agregando un "this." antes de cada propiedad del mismo. 